   // Iterate through the vector and find consecutive sequences
    for (int num : nums) {
        if (!table.contains(num - 1)) {
            int current_num = num;
            int current_length = 1;
            while (table.contains(current_num + 1)) {
                current_num++;
                current_length++;
            }
            max_length = std::max(max_length, current_length);
        }
    }
